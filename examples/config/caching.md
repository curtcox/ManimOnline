# `config/caching`

Enable/disable caching for faster re-renders.

Docs: [Configuration guide](https://docs.manim.community/en/stable/guides/configuration.html)

```ini
[CLI]
disable_caching = True
```
