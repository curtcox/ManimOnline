# `renderer/opengl`

Use GPU acceleration and OpenGL-specific constraints.

Docs: [OpenGL FAQ](https://docs.manim.community/en/stable/faq/opengl.html)

```bash
manim -pql --renderer=opengl example.py BasicScene
```
