# `geometry/line`

Draw straight segments between points.

Docs: [geometry](https://docs.manim.community/en/stable/reference/manim.mobject.geometry.html)

```py
from manim import *

class GeometryLineExample(Scene):
    def construct(self):
        shape = Line(LEFT, RIGHT)
        self.add(shape)
```
