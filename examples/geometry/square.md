# `geometry/square`

Create squares and rectangles.

Docs: [geometry](https://docs.manim.community/en/stable/reference/manim.mobject.geometry.html)

```py
from manim import *

class GeometrySquareExample(Scene):
    def construct(self):
        shape = Square()
        self.add(shape)
```
