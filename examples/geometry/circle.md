# `geometry/circle`

Create circles and arcs as VMobjects.

Docs: [geometry](https://docs.manim.community/en/stable/reference/manim.mobject.geometry.html)

```py
from manim import *

class GeometryCircleExample(Scene):
    def construct(self):
        shape = Circle()
        self.add(shape)
```
