# `geometry/dot`

Create points for anchors and markers.

Docs: [geometry](https://docs.manim.community/en/stable/reference/manim.mobject.geometry.html)

```py
from manim import *

class GeometryDotExample(Scene):
    def construct(self):
        shape = Dot()
        self.add(shape)
```
